# Funding links shown via the repo's "Sponsor" button.
# NOTE: handles below are placeholders — confirm/create these accounts, then
# update the values (and the donate links on the websites) before going live.
github: [projectmentor]                       # GitHub Sponsors (must be enabled on the account/org)
ko_fi: projectmentor                          # https://ko-fi.com/<handle>
custom:
  - https://www.buymeacoffee.com/projectmentor
